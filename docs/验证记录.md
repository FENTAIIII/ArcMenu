# 验证记录

本文件区分构建/数学验证与实际 Minecraft 客户端验收。

## 2026-09-04 · 第三方应用 API v1

| 项目 | 状态 |
| --- | --- |
| 独立构件 | 新增 `arcmenu-api` 模块；附属插件只以 `provided` 依赖公共契约，最终 Paper 插件仍内嵌 API，不要求服务器另装 API JAR |
| 注册与能力 | 唯一 namespaced ID、owner 所有权、重复冲突拒绝、单项 handle 注销、按插件批量注销、API 版本与能力枚举 |
| 应用会话 | 每玩家独立 factory/session/context；surface、pointer、主点击、scroll、tick、模式变化、surface 重定位和 close reason 均在服务端主线程回调 |
| 宿主边界 | 菜单与应用共用首次打开屏幕平面、摄像机、ArcMenu 光标、全屏 Interaction 和返回栈；应用不能建立第二套输入宿主 |
| 输入 | 鼠标模式与触控模式均使用右键；应用可切换 choose 光标；opt-in 鼠标滚轮把相邻热栏槽位及 `8↔0` 环绕归一化为 `+1/-1`，并取消真实持有槽改变；触控模式不捕获热栏 |
| 资源与异常 | `spawnPrivate` 在实体加入世界前设为私有；tracked entity/task/AutoCloseable 按生命周期强制清理；迟到异步结果由 `execute` 丢弃；应用回调异常关闭当前应用并尝试恢复来源界面 |
| YAML | 只接受 `open-app: namespace:path [arguments]`；明确拒绝 `openapp`、`app`、`open_app` 与方括号兼容语义 |
| 自动验证 | protocol 3、core 48、api 2、paper 57 项自动测试通过；Java 示例仅依赖独立 API 构件并以 `javac --release 21` 编译通过；Paper 1.21.1、1.21.4、1.21.6、1.21.11、26.1.2、26.2 六档 API 矩阵通过 |
| 实机状态 | **待第三方测试应用人工验收**：需要实际注册应用并验证菜单→应用→返回、两种输入、滚轮、世界切换、插件停用和异常恢复 |

## 2026-09-03 · M3 四窗口编辑器候选

| 项目 | 状态 |
| --- | --- |
| 参考与实现边界 | 阅读本地 Flashback 的 dockspace、透明主视图、宽高比适配、状态与历史组织；其许可证禁止再分发，因此新客户端以原生 Fabric Java Screen 独立实现，未复制其源码或资源 |
| 鼠标模式对接 | `/arcmenu edit` 无视运行时输入偏好，强制建立正对正式菜单的固定摄像机；Tab/属性刷新保留摄像机，编辑器关闭、断线或会话失效时恢复玩家视角；Fabric 系统鼠标负责编辑，不生成运行菜单假鼠标实体 |
| 工作区 | 工具、元素管理器、模板、属性四类窗口位于侧栏和底栏；中央开口尺寸为共同整数单位的 `16 × 9`；客户端使用 16:9 相机投影，将世界、正式 Display 和后处理组成的完整颜色帧缩放进开口，再绘制全不透明 dock；主目标先清屏，开口外不会残留全屏世界像素 |
| 虚拟屏幕校准 | 真实游戏帧继续占满中央 16:9 窗口；内部独立 16:9 校准矩形统一控制节点框、命中、拖拽和缩放坐标。客户端 `arcmenu-editor.properties` 提供 `virtual-screen-scale/offset-x/offset-y`，发行默认 scale 为 1.0，偏移使用 GUI 逻辑像素且 X 向右、Y 向上 |
| 前后端 | 前端与后端为独立 Tab；前端有层级、组与全部视觉工具，后端只有轴对齐点击区域；中央 16:9 窗口显示完整实时游戏帧，隐藏原版 HUD 和 Screen 默认暗幕；编辑预览走正式服务端渲染且不执行 actions |
| 工具与资源 | 支持组、矩形、边框、线、文本、图片、物品、方块和点击区域；图像工具展开服务端全部 PNG 候选，工具区可滚动 |
| 元素与属性 | 层级树支持展开、选择、本地锁定和 visible；服务端按类型下发 YAML 属性，Enter 提交并经过解析与渲染校验 |
| 模板 | 组可拖入模板窗命名保存，模板可拖回元素管理器实例化；根和后代 ID 冲突时依次追加数字，保证 YAML 键唯一；模板区可滚动 |
| 拖动预览 | 编辑协议 v5 为位置、前后端缩放和数值属性手势分配编号；客户端最多 20 次/秒且单请求在途，服务端逐次原地更新既有真实 Display，不重建菜单；中途只回紧凑 ACK，最终回完整快照；多次权威修订合并为一个撤销步骤 |
| 协议与事务 | 编辑协议 v5；支持前端元素与后端交互区域缩放、批量删除、复制、编组、同层排序和属性拖动事务；保留单菜单单写者、限流、修订冲突、100 步服务端撤销/重做、分块快照、外部文件修改保护、保存与应用分离 |
| 文件保护 | 写盘前重解析并比较语义，原子替换菜单；首次保存创建不覆盖的 `.arcmenu-backup`。结构与一般属性编辑会重新输出 YAML 排版，拖动标量仍保留原文本格式 |
| 自动验证 | `mvn -B -ntp clean verify` 通过 93 项：protocol 3、core 47、paper 43；Fabric `gradlew clean test build --warning-mode all` 通过 3 项布局/校准测试；Loom 编译验证 26.1.2 的 `GameRenderer.render/extractGui` 和 `Camera.update/createProjectionMatrixForCulling` 注入目标；新增实时拖动协议往返、同手势多采样仅一次撤销、触控/鼠标独立屏幕位置及虚拟屏幕配置持久化测试 |
| 产物 | 服务端 JAR 2,619,756 bytes，SHA-256 `963BAA908A1F389E72C0B2ACB6F454F568BE1CD13FC8DFF0BD3C1299DBC12D55`，已替换 toolserver 同名文件且哈希一致；客户端 JAR 92,175 bytes，SHA-256 `9038B7B599DC7F0DEB77C7F03A49D1F4CECA6A419E86F06772CFCAE6260045AD`；未启动或 reload 服务器 |
| 人工状态 | **候选**：需要客户端确认 `/arcmenu edit` 进入固定摄像机、完整真实帧被缩放进 16:9 窗口且四周零泄露，再检查正式元素拖拽、Tab 切换、图片候选、模板往返和全部属性 |

## 2026-09-03 · CraftEngine 真实物品示例

| 项目 | 状态 |
| --- | --- |
| CE 物品来源 | toolserver 新增的 CE `default_assets`，示例 ID 为 `default:topaz_sword`；该物品配置自身携带 `tooltip_style: minecraft:topaz` |
| 两种展示 | `examples/menus/ce-showcase.yml` 用同一 CE ItemStack 分别创建 `FIXED` 模型和 `GUI` 物品栏外观；示例单独复制到 toolserver，不成为通用发行包的强制 CE 依赖 |
| 黄玉 tooltip | 可选 `tooltip.yml skin` 将 CE 的 100×100 background/frame 构建成 9 个预合成切片；运行时按展开后的文字宽度、换行数、padding 和 min-size 动态布局，四角不拉伸；皮肤关闭时保持原单实体纯色路径 |
| Tooltip 九宫格锚点 | 修复把 TextDisplay 原点误当作文字左上角的问题：皮肤现以客户端实际的“缓存文字水平中心、文字块底边”为锚点；切片使用独立 ascent=2 字体并按实际不透明宽度抵消字形 advance 偏移。配置补充皮肤/文字独立位移、四边 padding、宽高补偿、缩放、接缝和逐行逐列微调；仍待实机像素校准 |
| Tooltip 动态锚点与长行 | 默认不使用 TextDisplay 自动换行，YAML tooltip 列表每项稳定对应一行，修复 180 字体像素临界处相差一个字符便改变布局的问题；显式 `wrap: true` 才按 `line-width` 换行。四种 `anchor` 将选定外框角固定到“指针 + offset”，内容宽高变化不再改变偏移基准 |
| toolserver 配置 | 两张精灵已复制到 ArcMenu `images/ce`，现有 crosshair 段保留用户设置的偏移并启用黄玉皮肤；因为旧文件没有独立 cursor 段，触控模式与鼠标模式共同使用该样式 |
| 自动测试 | `mvn -B -ntp clean verify` 通过，共 86 项：protocol 3、core 47、paper 36；新增可选/独立模式配置、非法尺寸、动态测量、PNG 图层合成、九切片尺寸与稳定码位测试 |
| toolserver 产物 | 已替换 `plugins/ArcMenu-0.1.0-SNAPSHOT.jar`；大小 2,543,674 bytes，SHA-256 `2B12BFD28F8C5A6D4EFAC687FE617A5986D304EE0DB2D162144EC523A2E3BEBE`；示例与 toolserver 副本哈希一致；未重启服务器 |
| 实机状态 | **待人工验收**：服务器重启并重建 CE 包后打开 `ce-showcase`，检查两种 context、短/长 tooltip 的九宫格接缝、遮挡与偏移 |

## 2026-09-03 · 触控模式/鼠标模式近眼屏幕候选

| 项目 | 状态 |
| --- | --- |
| CRServer 对照 | `phone.yml` 使用 `distance: 0.65`、`scale: 0.2`，3.4 格逻辑宽度的真实宽度约为 0.68 格；ArcMenu 据此把默认运行屏幕距离设为 0.65 格 |
| 全局屏幕变换 | `offset.yml` 的 `touch`、`mouse` 分别控制距离及屏幕局部 x/y/z 整体偏移；菜单、命中平面和捕获实体共用同一变换；编辑器因进入鼠标模式而使用 mouse 段，没有专用设置 |
| 比例与命中 | 运行时保持 `pixels-per-block * distance` 不变；默认 320×180、100 ppb、3 格距离转为约 461.538 ppb、0.65 格距离，真实宽度从 3.2 格降为约 0.693 格，视角大小、逻辑坐标与 backend 命中不变 |
| 路由与重建 | 首次打开、open/back 路由切换、传送/重生重锚定都使用近眼运行画布；菜单切换继续固定在首次观察点 |
| 鼠标模式摄像机对齐 | 固定摄像机从生物 `ArmorStand` 改为空的非生物 `TextDisplay`；避免客户端摄像机读取 LivingEntity 独立 head/body yaw 及姿态眼高后，与菜单法线产生近眼可见偏差；新实体的位置就是摄像机位置，只有一组 yaw/pitch |
| 角度量化对齐 | 用户截图四角为 `(416,300) (1082,297) (1083,674) (416,670)`，首轮量化修复后左右仍相差约 7 px。RCON 实测菜单平面方向为 `-73.125°`，摄像机 NBT 却因“桶中心”策略保存为 `-72.421875°`，两者恒差 `0.703125°`；26.1.2 的完整位置同步包用 float 传角度，因此桶中心会覆盖生成包的 byte 角度。现让菜单平面和摄像机实体都直接保存同一个可编码角度；穷举 256 个角度确认再次经过 byte 编码仍为自身 |
| Tooltip 深度 | RCON 实测当前菜单 13 个 TextDisplay 中只有 tooltip 的 `see_through` 为 `1b`；该路径会绕过正常深度测试，并在水等透明世界几何后显出清晰地形。现统一改为 `false`，背景 alpha 仍由 `tooltip.yml` 的 `#AARRGGBB` 决定 |
| 配置与术语 | 新增独立 `offset.yml`；touch/mouse 距离默认均为 0.65，可分别设置，整体偏移支持 x 向右、y 向上、z 向观察者；对外名称统一为“触控模式”与“鼠标模式”，旧 crosshair/cursor 值仅作兼容别名 |
| 自动测试 | 当前 `mvn -B -ntp clean verify` 通过，共 93 项：protocol 3、core 47、paper 43；覆盖视角比例不变、触控/鼠标独立距离、三轴整体偏移、默认值/严格校验及摄像机角度量化 |
| toolserver 产物 | 已替换 `plugins/ArcMenu-0.1.0-SNAPSHOT.jar`；SHA-256 `963BAA908A1F389E72C0B2ACB6F454F568BE1CD13FC8DFF0BD3C1299DBC12D55`；源/目标哈希一致；`plugins/ArcMenu/offset.yml` 已写入；未由构建流程 reload 或重启服务器 |
| 实机状态 | **待人工验收**：需在普通和狭小空间分别打开触控模式、鼠标模式，确认屏幕真实尺寸、遮挡、鼠标尖端和服务端命中位置 |

## 2026-09-02 · M6 固定摄像机协议重做候选

| 项目 | 状态 |
| --- | --- |
| 上一候选处置 | 用户判定基于 `PlayerMoveEvent` 的持续视角回拉方案严重不通过；该实现已删除，不作为 M6 能力保留 |
| 摄像机 | 每玩家私有、不可见、固定 ArmorStand 摄像机；camera 包切换客户端渲染目标，进入时按菜单平面法线正对中心；关闭先切回玩家再移除实体 |
| 摄像机包构造 | 修复实机报错 `Cannot create instance of class ClientboundSetCameraPacket`：不再创建不存在的无参包，改由 ProtocolLib 将 Bukkit 实体解包并调用 `ClientboundSetCameraPacket(Entity)`；进入、跨世界重建和退出三条路径均使用实体构造器 |
| 协议输入 | 外部 camera target 会令原版客户端停止自然发送 LOOK；现改为每 tick 发送不改变位置/旋转的客户端相对位置同步，读取其立即返回的 PosRot，并吞掉对应负数 teleport ACK 与回包；旋转差值只更新虚拟光标，不进入服务端玩家位置/朝向；pitch ±85° 只重置隐藏客户端旋转 |
| 鼠标图形与手部 | 固定读取 `images/mouse/mouse.png`（20×30），独立私有 TextDisplay、alpha 255、普通文字渲染；箭头尖端按 PNG 左上角对齐服务端命中点；运行期仅向该客户端发送 spectator 渲染状态以隐藏手和手持物，关闭前恢复隐藏旋转、切回玩家并恢复玩家真实游戏模式 |
| 自身交互防护 | 鼠标模式期间在 ProtocolLib 收包层取消目标实体 ID 为玩家自身的 USE_ENTITY，避免摄像机与玩家重叠时触发服务端 `Cannot interact with self!` 断线；菜单 Interaction 实体的右键包不受影响 |
| Tooltip 配置兼容 | 修复旧版 `tooltip.yml` 只有 `crosshair` 段时，鼠标模式悄悄使用硬编码 `cursor` 默认值的问题；缺少整个 `cursor` 段时现完整继承 `crosshair`，显式配置 `cursor` 时仍保持两套样式独立 |
| 动画变换稳定性 | 无剪切矩阵改用确定性 T-R-S 分解，逐实体保持四元数同半球，避免缩放接近 1 或循环越过 360° 时 Minecraft 的 SVD 选择另一组等价旋转而整圈翻转；含剪切帧保持精确矩阵并停用该帧客户端旋转插值；每个新 Display 的首个动态目标只建立客户端基线，从第二个目标才允许插值，避免快速切换时从客户端默认朝向绕世界竖直轴转到菜单朝向 |
| 退出规则 | 运行菜单不因移动、距离、权限、传送、死亡或切世界关闭；传送/重生重建世界实体且不运行 close/open；仅显式关闭、菜单 `close`、退出及插件停用清理 |
| 自动测试 | `mvn -B -ntp clean verify` 通过，共 77 项：protocol 3、core 47、paper 27；新增测试覆盖过渡缩放到单位矩阵、连续两轮 360° 周期接缝、剪切回退和新实体首次动态目标的无插值基线 |
| 版本矩阵 | 加入 ProtocolLib 5.4 编译依赖后，1.21.1、1.21.4、1.21.6、1.21.11、26.1.2、26.2 全部通过；修复矩阵脚本把 `java -version` stderr 误判为失败的问题 |
| toolserver 产物 | 已替换 `plugins/ArcMenu-0.1.0-SNAPSHOT.jar`；SHA-256 `B0E52DA5B823BF46B60749E73A812AFE83E45898326EB97D72387616DB2A6053`；ProtocolLib 类未打入产物；未启动服务器 |
| M6 总状态 | **重做候选，必须人工确认固定镜头、鼠标尖端命中、pitch 重置、传送/重生保持和关闭恢复后才能通过** |

## 2026-09-01 · M6 版本与体验候选

| 项目 | 状态 |
| --- | --- |
| 鼠标模式边界 | 独立 `PointerController` 输出统一菜单坐标；命中、tooltip、actions 与动画不含模式分支；普通玩家无需模组 |
| 玩家体验 | player-choice/force-crosshair/force-cursor、PDC 偏好、独立 cursor tooltip、完全不透明私有光标、灵敏度/边缘夹取及 Java API 已实现 |
| 原型限制 | 使用原版 look 增量并纠正位置/视角；系统准星无法按玩家隐藏，镜头观感和插件冲突待实机，不声明原生 GUI 体验 |
| 版本矩阵 | 1.21.1、1.21.4、1.21.6、1.21.11 在 JDK 21.0.9 通过；26.1.2、26.2 在 JDK 25.0.2 通过；均为 API 编译与自动测试，不是服务器运行 |
| 性能观测 | `/arcmenu performance [reset]` 记录 ticker 耗时、矩阵/元数据写入与抑制、实体创建；明确不冒充 bytes/MSPT/FPS |
| 可复现负载 | 提供 20/100/300 静态节点菜单生成脚本；生成的 20 节点 YAML 已由 YAML 解析器验证 |
| core 微基线 | 独立 JVM：20/100/300 节点 SceneCompiler median 为 4.121/10.742/35.396 µs，p95 为 8.333/17.973/41.387 µs；仅用于同机回归 |
| 优化划分 | 统一 ticker、静态/矩阵/元数据抑制、缓存、清理、指针准确性和测量入口列为开放核心；赞助候选必须另有同负载收益证据 |
| 自动测试 | `mvn -B -ntp clean verify` 通过，共 69 项：protocol 3、core 47、paper 19；benchmark 模块构建通过 |
| toolserver 产物 | 已替换 `plugins/ArcMenu-0.1.0-SNAPSHOT.jar`；SHA-256 `6F62C889636C6FC72B043243829193445FA3ED216485F3F74944747E1442FE25`；未启动服务器 |
| M6 总状态 | **严重不通过，已被 2026-09-02 的固定摄像机协议实现替代** |

## 2026-09-01 · M5 动画与 API 候选

| 项目 | 状态 |
| --- | --- |
| 动画配置 | 独立 `animations.yml` 严格解析；菜单/目标/属性/关键帧/动作引用交叉校验；`validate` 不应用，`reload` 与菜单原子替换 |
| 菜单过渡 | enter/switch/exit 根变换已接入；切换继续复用首次打开平面；过渡期间暂停业务点击，退出末帧后清理实体 |
| 节点轨道 | offset/rotation/scale、文本 content/opacity、四种 easing、once/repeat/ping-pong 和 open/API 触发已实现；group 变换传递到视觉子节点，后端固定 |
| 所有权与清理 | 同一节点属性的新 API 轨道替换旧所有者；stop 恢复 YAML 基础值；关闭先取消节点轨道；生命周期关闭会清除 close event 可能新开的替代会话 |
| 渲染成本 | 复用既有 Display，只在单个实体矩阵实际变化时提交更新；变换轨道不会每 tick 创建实体或重启静态实体插值，也不会反复展开无关静态文字 Placeholder |
| Java API | Bukkit Services API、Java SAM 路由、主线程限制、参数副本、插件禁用清理已实现；打包 ABI 由 `javap` 检查，示例 Java 附属源码由 JDK 21 `javac` 编译通过 |
| 示例 | 分发 `m5-animation.yml` 与 `animations.yml`；覆盖过渡、旋转模型、文本内容/透明度、group 平移/缩放、动画动作、关闭和公共路由 |
| 自动测试 | `mvn -B -ntp clean verify` 通过，共 66 项：protocol 3、core 47、paper 16 |
| toolserver 产物 | 已替换 `plugins/ArcMenu-0.1.0-SNAPSHOT.jar`；SHA-256 `AD8CA973349F4D9CE9AD97D30EA1FB02B067C6C00D9F5900EABEDEEC1CE1AFF8`；未启动服务器 |
| M5 总状态 | 2026-09-01 用户确认可以进入 M6，M5 标记为通过；期间发现的静态背景/暂停模型重复重启插值问题已改为逐实体矩阵变化抑制 |

## 2026-09-01 · M4 资源候选

| 项目 | 状态 |
| --- | --- |
| M3 处置 | 用户判定第一版编辑器不合格；旧入口停用，阶段未通过，延期到鼠标模式阶段重做 |
| 图片资源 | `images/` 递归索引、PNG/路径/大小限制、稳定私用码位、`arcmenu:images` 专用字体、展开目录与 ZIP 已实现 |
| CraftEngine 合包 | 按 BetterHud 的方向接入 `AsyncResourcePackCacheEvent`，通过 CE API 注册外部 ZIP；待 toolserver 执行 `/ce reload all` 实测 |
| 图片节点 | 固有或显式尺寸、静态 opacity、动态路径及 update 已实现；待客户端检查基线、中心和比例 |
| CE 物品 | 原版优先、CE ID 回退、按玩家构建 ItemStack 并进入 ItemDisplay context；待真实 CE 物品实测 |
| 组模板 | 目录原子校验、单 group 根、禁止 backend、复制实例生成新 ID 已实现；旧 Fabric 插入界面不开放 |
| 自动测试 | 58 项通过，其中新增模板 3 项、资源包 4 项、CraftEngine 桥接 1 项，并更新图片解析、绝对合包路径与完整示例覆盖 |
| M4 总状态 | **候选，等待 CE 合包、图片画面与 CE 模型人工验收** |

## 2026-09-01 · M3 坐标闭环候选

| 项目 | 状态 |
| --- | --- |
| Paper 插件 | JDK 21 字节码，`mvn -B -ntp clean verify` 通过 |
| Fabric 管理员模组 | Minecraft 26.1.2、Loader 0.19.5、Fabric API 0.155.2+26.1.2、Fabric Language Kotlin 1.13.13；Gradle 9.5 / Loom 1.17.20 `clean build` 通过 |
| 自动测试 | 49 项通过：既有 M1/M2 40 项、编辑协议 3 项、服务端草稿 3 项、服务端坐标 3 项 |
| 编辑协议 | v1；消息大小/分块、单菜单单写入者、权限/频率/修订校验、服务端权威快照已实现 |
| 两个编辑器 | 前端与后端为独立 Tab；切换清除选择；后端没有父组且始终轴对齐 |
| 拖拽事务 | 本地逐帧预览、松开提交一次；前端按父组逆矩阵写局部 offset，后端移动/右下手柄缩放 |
| 草稿与保存 | 100 步服务端撤销/重做；保存与应用分离；原子写盘；外部修改冲突；标量补丁保留未编辑文本并在重解析语义不同时拒绝写盘 |
| 坐标探针 | 客户端显示 GUI/帧缓冲/客户端逻辑坐标；服务端依据鼠标与 viewport 独立重算并返回命中，世界黄色十字显示采纳点 |
| 实机结果 | 用户判定第一版编辑器方案不合格；本阶段未通过并已延期 |

自动坐标测试覆盖中心、四角、客户端伪造逻辑坐标不影响服务端结果、非法 viewport/非有限值/远端点拒绝。自动草稿测试覆盖旋转 90° 父组内的全局拖动、后端轴对齐、修订冲突、撤销/重做、注释/锚点/动作保留和外部磁盘修改保护。

## 2026-09-01 · M2 候选

| 项目 | 状态 |
| --- | --- |
| JDK 21 `clean verify` | 已通过，40 项测试，产出 Java 21 字节码候选 JAR |
| Paper 26.2 API 编译 | JDK 25 + `26.2.build.111-stable` 编译通过；不是服务端运行验收 |
| TrMenu 风格动作/条件解析、fixtures、静态路由检查 | 自动测试通过；兼容边界见 `TrMenu-M2-兼容表.md` |
| CRServer 风格右键捕获 | 已实现私有 Interaction、主手过滤、空气/方块/实体统一入口和 75ms 去重；待游戏内验收 |
| 左键行为 | 只取消对 ArcMenu 捕获面的伤害，不注册动作；待游戏内验收 |
| 动态文字、PlaceholderAPI、普通 tooltip、update | 已实现；待观察显示位置、刷新与无变化抑制 |
| TrMenu 适用动作 | 常用消息/命令/声音/路由、动作选项、延时、参数、meta/data、原版物品、可选经济桥及 CHAT catcher 已实现；副作用必须在测试服人工核对 |
| 声音枚举兼容 | `BLOCK_CHEST_OPEN`、`UI_BUTTON_CLICK` 等旧枚举名会归一到现代 registry key；待 toolserver 确认不再告警 |
| 菜单切换平面 | open/back/重复 open 复用首次打开原点与朝向；自动几何测试通过，待客户端确认切换不移屏 |
| PlayerLoginEvent 警告 | ArcMenu 源码与监听器没有注册 PlayerLoginEvent；用户日志列出的监听插件为 CMI、ProtocolLib，不能由 ArcMenu 内部修复 |
| 会话/捕获面/显示实体清理 | 生命周期路径已覆盖代码与既有测试边界；仍需游戏内重复开关检查 |
| M2 总状态 | 2026-09-01 用户确认可以进入 M3，M2 阶段标记为通过；未逐版本实测的兼容范围仍保持未验证 |

## 2026-08-31 · M1

| 项目 | 状态 |
| --- | --- |
| M1 阶段结论 | 2026-09-01 用户确认可以推进 M2，M1 标记为通过 |
| Kotlin 2.4.10、Paper 1.21.1 API、JDK 25 编译和单元测试 | 已通过，31 项测试 |
| JDK 21 基线打包 | `mvn -B -ntp clean verify` 已通过，31 项测试，产出 Java 21 字节码 JAR |
| Paper 26.2 API 编译检查 | JDK 25 + `26.2.build.111-stable` 编译通过；不是服务端运行验收 |
| 原版客户端实际画面、实体可见性、事件捕获与生命周期 | 待游戏内验收 |
| Fabric 编辑器 | 尚未构建 |
| 用户鼠标/服务端坐标人工审阅 | 待匹配的 Fabric 与插件构建后进行 |
| CE 资源包、TrMenu 动作语义、动态刷新、动画 | 尚未实现，不标为通过 |
| 1.21.1–26.2 全范围兼容与性能数据 | 待逐版本测试 |

测试覆盖：前端嵌套变换不改变后端；后端非法分组与变换属性；全菜单 ID 唯一；重复 YAML 键；拼写错误和非法数值；原始业务表达式保存；不同朝向下的世界射线与逻辑坐标；背面/平行/非有限射线；矩形边界与优先级；隐藏前端独立于后端；四边框图元成本；配置及渲染属性校验失败时保留旧目录快照；菜单删除和重复菜单 ID；随 JAR 分发的示例。

M1 测试不启动 Minecraft，不执行配置中的业务命令。2026-09-01 根据 toolserver 的 `real_border1` 实机数据，将 frame 改为按基准比例缩放边缘位置并让竖边走旋转 line 路径；同时锁定 `offset.z: 1` 在默认画布中等于 0.01 世界方块，示例覆盖 M1 支持的 line、frame、rectangle、text、item、block、group。用户随后明确允许推进 M2，因此 M1 阶段已通过；验证表中尚未逐项实测的版本和性能范围仍保持原状态，不随阶段通过自动改成已验证。

## 后续人工检查清单

1. 在匹配版本的测试服务器安装插件，记录 Paper/Java/客户端版本；确认普通其他玩家看不到管理员预览。
2. 打开 example 的前端与后端预览，确认后端不随 15° 前端卡片旋转。用 `/arcmenu pointer` 检查中心和矩形边界。
3. 重复打开、关闭、传送、切世界、死亡和退出，检查 `/arcmenu status` 中会话/实体计数回落，画面无遗留实体。
4. 将 YAML 改为无效并 reload，旧预览仍可用；修正后 reload，重新打开查看更新。
5. Fabric 构建交付后，再由用户检查系统鼠标像素、逻辑坐标、服务端独立回传坐标与显示标记的一致性；覆盖 FOV、GUI 缩放、宽高比、Tab 切换、拖动、撤销和高延迟。

第 5 项是用户明确要求的人工验收，前四项和单元测试不能替代。
